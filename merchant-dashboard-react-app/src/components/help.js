import React from 'react';
import { Divider, Input, Calendar, Avatar } from 'antd';
const { TextArea } = Input;
const onChange = e => {
    console.log(e);
};
  
const Help = () => {
    return (
        <div>
        ...
        </div>
    );
};
        
export default Help;